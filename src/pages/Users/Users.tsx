import { Table } from 'antd';
import { FC } from 'react';
interface UsersProps {}
const Users: FC<UsersProps> = ({}) => {
  const columns = [{ title: 'Foo' }];
  return <div>Users</div>;
};
export default Users;
